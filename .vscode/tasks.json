{
	"version": "2.0.0",
	"tasks": [
		{
            "label": "Build plugin",
            "type": "shell",
            "command": "cd src/main && go build -buildmode=plugin -gcflags all='-N -l' ../mrapps/wc.go",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
	]
}